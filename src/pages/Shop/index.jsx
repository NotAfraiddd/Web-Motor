import styled from 'styled-components';

const StyledSection = styled.section`
    min-height: 100vh;
    height: auto;
    width: 100vw;
    margin: 0 auto;
    overflow: hidden;

    display: flex;
    justify-content: flex-start;
    align-items: flex-start;
`;

function PageShop() {
    return <StyledSection>PageShop app</StyledSection>;
}

export default PageShop;
